# Chart guidance

Load this resource only for an explicit chart request.

- Match the chart form to the analytical question.
- Label axes and units.
- Preserve missing values rather than silently coercing them to zero.
- Include the source values or a reproducible data table.
- Verify the rendering separately from the underlying calculation.
